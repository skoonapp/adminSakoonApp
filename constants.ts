// This file contains constant values used across the application.

// Rates used for calculating earnings.
export const CALL_RATE = 10;         // ₹10 per minute (gross)
export const CHAT_RATE = 0.5;        // ₹0.5 per message

// The percentage of the gross earnings that the listener receives.
export const LISTENER_SHARE = 0.35;  // 35% listener share
